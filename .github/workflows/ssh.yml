name: Ubuntu
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  watch:
    types: started
    
jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    steps:
    - name: SSH connection to Actions
      uses: P3TERX/debugger-action@master
